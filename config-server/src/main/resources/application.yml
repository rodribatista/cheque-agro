server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rodribatista/DH_back-E1.git
          search-paths: config-data
          username: ${GIT_USERNAME}
          password: ${GIT_TOKEN}
          default-label: "main"