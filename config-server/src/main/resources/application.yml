server:
  port: ${PORT:8888}

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rodribatista/config.git
          #search-paths: config-data
          #username: ${GIT_USERNAME}
          #password: ${GIT_PASSWORD}